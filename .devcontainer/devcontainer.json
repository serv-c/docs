{
  "image": "mcr.microsoft.com/vscode/devcontainers/python",
  "postStartCommand": "pip install -r requirements.txt && mkdocs serve",
  "customizations": {
    "vscode": {
      "extensions": [
        "GitHub.copilot",
        "ms-python.python",
        "bierner.markdown-mermaid"
      ]
    }
  },
  "postCreateCommand": "pip install -r requirements.txt && mkdocs serve"
}
